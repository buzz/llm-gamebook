import { IconPlus } from '@tabler/icons-react'

import PageShell from '@/components/layout/PageShell'

function ProjectForm() {
  return (
    <PageShell icon={IconPlus} title="New Gamebook">
      ProjectForm
    </PageShell>
  )
}

export default ProjectForm
